; fmt is used directly by the rule messages; it was previously reaching the
; rules only through lintocaml_engine's own dependency on it.

(library
 (name lintocaml_rules)
 (libraries lintocaml_engine fmt))
